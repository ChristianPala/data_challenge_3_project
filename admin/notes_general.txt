- Letters at the end of the stock code, is there a pattern?

[DONE] trasformare tutti stock code con le lettere a integers (semplicemente togliere la lettera dallo stock code

[DONE: (df.dropna())] se ci sono stock code che non hanno mai nessuna descrizione:
            - droppiamo direttamente la row,
            oppure
            - lasciamo la descrizione vuota se decidiamo che non è importante

[DONE] fare la funzione per cercare dei matching negli invoice codes (se un ordine con la C (ordine stornato) non ha un matching "true" ordine,
            droppiamo solo l'ordine con la C, altrimenti si droppano sia l'ordine originale che quello stornato)

[DONE] droppare tutti i customer id NULL

[DONE] replace delle virgole con "." nelle quantità dei prodotti


ho modificato alcune righe dei files della libreria TSFEL: (non so se riesco a trovare un metodo di modificare il passaggio di dati invece che la libreria...)

/!\ volendo puoi copiare il file time_series/modded_lib_files/features.py, è quello che ho modificato e se lo runni dimmi se ti da errori
mettilo in questa path: <your install dir>/anaconda3/envs/<your env name>/lib/python3.10/site-packages/tsfel/feature_extraction/

    - features.py, linea 1511: ho aggiunto il check cosicche la frequency viene cappata a 0 se il parametro diventa negativo

    - features.py, linea 784:  ```try:
                                      return tuple([x[y <= p].max() for p in percentile])  # yes I seriously used this shit
                                  except ValueError:
                                      pass```

also, ci mette una vita a fare il feature extraction per ogni customer...